# AI 设定工作流可靠性修复

## 范围与保留行为

本次修复覆盖快速设定、逐步引导、单项三选一、用户修改后的 AI 复核、草案确认和相关计费。
保留 Vue/FastAPI 技术栈、四种剧本类型、已有正式设定和场次内容；不自动部署、不改写历史用量。

## 分批交付

| 批次 | 目标版本 | 范围 | 状态 |
| --- | --- | --- | --- |
| 1 | 0.0.1 | 原子版本控制、过期缓存、跨项目请求隔离、生成期间写入保护 | 已完成，66b2164 已核对远端 |
| 2 | 0.0.2 | 字段校验、单位解析、三选一有界补齐、AI 输出和用量错误契约 | 已完成，c9083bc 已核对远端 |
| 3 | 0.0.3 | 可保存工作稿、切换保护、用户改动方向与联动复核 | 已完成，f007de7 已核对远端 |
| 4 | 0.0.4 | 计费主体统一、备份兼容、完整回归 | 已完成（0.0.4 交付提交） |
| 5 | 0.0.5 | 移动端头部修复、PWA 安装、静态离线壳和安全更新 | 验证与独立复审通过，待同步 |

每批通过相应自动化验证、页面检查和独立审查后，更新本记录并同步仓库。
页面版本由 `frontend/package.json` 唯一管理，Vite 在构建时注入；本地版本不代表线上已部署。

## 验收不变量

- 请求固定项目、版本和输入快照；旧响应不得污染另一个项目或清除新请求状态。
- 设定写入须通过数据库原子版本条件；旧会话不能覆盖新会话，活动生成任务期间不得修改基础设定。
- 单项操作只产生候选；三个选项必须是有效且不同的新值，不能以重复或占位内容凑数。
- 用户明确应用或确认前，不将 AI 候选写入正式设定；本地冲突时禁止部分应用。
- 计费检查与累计主体一致；已知用量包括失败、补齐和过期请求，不伪装为零消耗。
- 测试使用隔离数据库和明确标识的模拟 AI；真实模型成功率、线上部署和视觉验证分别报告。

## 验证记录

实施前已有后端测试 86 项通过，前端类型检查通过；它们没有覆盖后续审计复现的全部边界。
本次各批新增的验证与实际结果会在对应批次完成时追加。

### 第 1 批：0.0.0 → 0.0.1

- 新增双版本 CAS 边界，覆盖设定写入、分析缓存、历史恢复及生成任务认领。
- 修复旧上下文写回、SQLite 场次 ID 复用后的目标错位和快照失真；场次提示词只在短事务内合并最新内部缓存。
- 前端请求固定项目、版本、通道与生命周期；补齐退出、删除、详情失败、旧后续请求和 loading 恢复。
- 后端全量 153 项、前端异步 13 项、Vue 类型检查及生产构建通过；独立代码审查通过，无本批遗留 P1/P2。
- 本地隔离后端配合模拟 AI，实际查看登录、草案恢复、标题三选一及应用；1280 像素桌面和 390×844 移动端实看通过，移动端文档宽度等于视口宽度。
- 登录页、桌面侧栏和移动菜单中的版本号实际核对为 0.0.1。
- 未验证真实 AI 成功率、PostgreSQL 实库并发或生产升级；本批没有部署。构建仍提示既有 Browserslist 数据较旧，不影响构建结果。
- 审查发现的共享写入及请求生命周期问题已修复并回归；第 2—4 批已知问题仍待后续实施。
- 仓库同步：`66b2164`，已推送并核对 `origin/main`。

### 第 2 批：0.0.1 → 0.0.2

- 字段安全、单位、标题与最终确认共享纯规则；43 个类型/目标组合允许修复空或无效目标，锁定内容保持原文。
- 时长精确换算、计数严格校验；标题内部标点保留，前后端均支持明确前缀及嵌套包装；补充说明允许“无”。
- 选项保留有效项并最多补齐一次，模型提示携带同源约束和有界拒绝原因；不生成假选项。
- 复杂字段只保守拒绝明显泛化模板，不用固定动词/职业词表否定既有具体设定。
- 已知用量、原始模型返回和失败原因可审计；审计存储故障明确 503，不应用结果、不额外调用模型，已知项目用量保留。
- 全量后端 271 项、前端 18 项及生产构建通过；独立审查的标题展示、占位筛选、审计失败三项已修复并回归。
- 本地浏览器实看：模拟首次返回旧标题+重复标题，第二次补齐后展示三个有效新标题，5+7 的模拟用量显示为 12；短剧 1.5 分钟在桌面及移动端详情完整显示。
- 0.0.2 版本已实际核对；390 像素移动端无横向溢出。未调用真实收费 AI，未验证生产存储故障恢复或 PostgreSQL 实库，本批未部署。
- 仓库同步：`c9083bc`，已推送并核对 `origin/main`。

### 第 3 批：0.0.2 → 0.0.3

- 新增独立工作稿与 `0006` 迁移；保存只更新稿和缓存版本，不写正式标题、类型或确认标志。
- 保存、放弃、模式切换与整份重生有明确动作；仅模式切换保留稿有效性，正式设定变化使旧稿只读，不自动合并。
- 生成基线在重复保存中保持不变；AI 复核收到真实 before/after，联动模式锁定用户及 AI 选择的新方向和规模条件。
- 前端保留保存请求期间的后续编辑；放弃未保存修改不删除服务器已存稿；本地冲突只读稿仍受离开/刷新保护，强制登出不触发保存循环。
- 后端全量 315 项、前端异步 30 项及生产构建通过；第 3 批独立审查通过。
- 隔离浏览器实看保存标题后刷新/服务重启恢复、保存切 guided 后返回同稿、正式类型变化后 guided 继续下一问、显式返回旧稿只读且禁用保存/确认/AI。
- 桌面与 390×844 移动端查看保存/过期状态通过，版本为 0.0.3，无横向溢出。实际查看后补齐了 guided 返回入口的顶层标志传递，并隐藏过期稿上矛盾的通用操作说明。
- 无真实 AI 调用、无生产部署；PostgreSQL 实库并发/迁移仍未提供验证环境。
- 仓库同步：`f007de7`，已推送并核对 `origin/main`。

### 第 4 批：0.0.3 → 0.0.4

- 新增 nullable 计费对象和 `0007` 迁移，保留实际操作者；项目 AI 归所有者、独立审核归操作者，历史 NULL 与时间戳不回填。
- 所有项目调用和 Worker/重试传递身份；业务补齐、provider 重试及备用配置调用前均复核最新额度，请求局部上下文不会串账。
- 静态问题、有效缓存、工作稿和完成/确认查询不受额度阻断；可选摘要在额度不足时降级而不阻断确认。
- 无效审核/摘要/空内容失败不记假成功；连续性拒绝和提示词末端 CAS 冲突修正原审计状态，不重复计费。
- 导出、加密备份和导入保留计费 ID、工作稿及双版本；跨环境用户重映射与缺失映射回滚、旧包兼容均有测试。
- 全量后端 361 项、前端 33 项及生产构建通过；最终独立审查通过，无本批未解决 P1/P2。
- 实际本地页面核对：编辑者触发的项目 AI 记录将操作者与项目所有者分列；旧记录标“历史记账”。0.0.4 在创作室、登录页及后台共用同一版本来源。
- 1280 像素桌面与 390×844 手机后台实看通过；宽审计表在组件内横向滚动，页面无横向溢出。

### 第 5 批：0.0.4 → 0.0.5

- 移动端头部的品牌改为不可收缩的单行横排；窄屏将项目工具、导出和新创意收敛为带名称、焦点态和至少 44px 触控区域的图标按钮，桌面仍显示文字按钮。
- 新增 Manifest、192/512 图标、Apple Touch Icon、maskable 图标、离线说明页和版本化 Service Worker。PWA 只预缓存明确的静态资源白名单；项目、认证、管理、AI、带授权头、带查询参数及跨源请求都不由缓存响应。
- 安装入口位于账户菜单和移动抽屉；iOS 提供“Safari 分享 → 添加到主屏幕”说明。普通关闭页与 PWA 更新共用快速草案、创意、自定义输入和已选选项的未保存判定。
- 等待中的 Worker 与用户确认绑定；其他标签更新只提示当前页可刷新。“保存并更新”后复查新编辑；真正刷新前再校验批准快照，匹配时一次性豁免原生离开提示。激活期间继续编辑则保留内容与刷新入口。
- 构建后以严格路径白名单生成 SW，解码后再次拒绝查询/片段及路径逃逸；只处理两个壳导航，其他导航与所有 API 直通。开发服务器不注册生产 SW。离线停止轮询并阻止需联网操作。
- 部署与更新脚本校验 Vite 8 所需 Node 版本；默认二进制升级为已核实的 22.23.2 安全补丁。完整构建或只读 SW 产物校验失败即停止，无降级绕过检查。
- 前端 56 项、隔离后端 367 项、类型检查、生产构建、PWA 产物和脚本语法检查通过。包含 SW 实际 fetch 事件矩阵、多标签状态、原生离开许可与构建失败阻止后续步骤的测试；本批独立复审通过。
- 真实独立 Chromium 在 320/360/390/768/1280px 验证已登录头部：完整横排品牌、按钮至少 44px、头部和文档宽度等于视口；登录、桌面侧栏与移动菜单版本沿用同一 0.0.5 来源。Manifest 和可安装条件检查无错误，真实 Cache Storage 无 API，冷启动离线页及重连通过。
- 显示为 96/32px 的 Logo 复用 192px 小图，资源从 1,494,583 字节降至 22,916 字节；原图保留且移出必需预缓存。最终 SW 14 项。PNG 尺寸为 192×192、512×512、180×180。
- 本地同条件各 3 次首屏实验：390×844、4 倍 CPU 降速、100ms 延迟、200,000 B/s 下载、关闭 HTTP 缓存与 SW（隔离首次加载影响）。LCP 中位数 12.772s → 5.564s；资源载荷 2,323,246 → 856,919 字节，CLS 均为 0。初次测量有一轮等待网络空闲超时；改为等待实际首屏图片就绪后完成对比。不以实验室数字声称线上同等提升。
- Mac 锁屏造成的上一轮浏览器缺口已用独立临时浏览器补验；没有实体 iPhone 安装/安全区验证、线上部署、真实 AI 或 PostgreSQL 实库验收。全项目复核的其他既有风险另作分批优化方案，不记为本批已修。

## 最终边界与运维注意

- 本次完成代码、测试、页面验收及已授权仓库同步；未部署线上、未调用真实收费 AI。
- 真实模型成功率和 PostgreSQL 实库并发/迁移未验证，不能把模拟测试等同真实模型质量。
- 配额为软额度，已在途并发可能超过阈值；没有硬预留账本。
- 审计存储故障会明确 503，不应用本次结果；现存项目的已知累计 Token 保留，但账户用量按审计统计，故障时可能缺失记录。本次没有 outbox 或自动补记；应先恢复存储并核对上游账单，勿反复发起模型调用代替修复。
- 构建存在既有 Browserslist 数据较旧提示，构建成功；本轮未进行无关依赖升级。
- 上线前先备份，前后端成套更新并执行 Alembic 迁移；Git 推送不等于生产部署。版本与提交对应关系以 `frontend/package.json` 的 Git 历史及交付提交为准。
