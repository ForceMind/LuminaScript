# 全项目复核与优化方案

检查日期：2026-09-06。检查起点：`main@f1286e7` 及当时尚未提交的 0.0.5 移动端与 PWA 改动；当前交付状态见下文。

## 结论与当前范围

全项目审查尚未全部通过。已完成生成准入、重试保留、租约围栏、SQLite 外键、审计删除语义、用量聚合和任务权限分页；剩余高优先风险集中在 LLM 调用边界、认证阻塞、公开注册费用控制、完整生成状态机和大对象轮询。

本轮沿用已批准的移动头部/PWA 修复和 `ForceMind/LuminaScript` 的 `main` 同步范围。0.0.5 已以 `40eeb08` 推送并核对远端；0.0.6 已以 `ac296f1` 推送并核对远端；生成保护 0.0.7 已以 `e078946` 推送，数据库一致性 0.0.8 已以 `26f76d3` 推送，二者均已核对远端。下文区分已修复与尚未实施项。

审查覆盖：Vue 创作室及管理员页面，认证与权限，设定/草案/生成工作流，LLM 调用，用量及审计，任务队列，数据库迁移，版本与备份，构建和部署脚本。没有访问生产系统、读取真实作品、调用收费模型或执行线上迁移。

## 证据与限制

- 隔离后端回归：385 项通过，包含生成准入、重试保留、租约/取消、旧 SQLite 外键迁移、查询计划和权限分页的行为用例。测试数据库独立创建。
- 原有测试通过不代表以下反例已被覆盖：重试清空场次、未确认设定入队、任务权限分页和 SQLite 外键状态均另作了隔离验证。
- 前端代码分别由实现者和独立审查者检查；生成的 Service Worker 在测试中实际执行 fetch 事件矩阵，不只检查字符串。
- 独立 Chromium 151 使用本地合成 API 经正常登录进入作品页。320、360、390、768、1280px 下文档与头部均不横向溢出，品牌横排，触控按钮不小于 44px。图片与截图实际查看。
- PWA Manifest 解析与可安装条件检查无错误；Cache Storage 只含静态资源，没有 API 请求；冷启动断网显示离线说明页，恢复网络可重新打开。
- 浏览器测量是本地实验室数据；没有生产 LCP/INP/CLS 用户分布、真实设备或网络负载数据。指标定义参考 [Web Vitals](https://web.dev/articles/vitals)，不能把单机测试当作线上性能承诺。
- `npm audit` 初查 5 个依赖包有公告：2 high、2 moderate、1 low。`pip-audit` 对本次实际使用的隔离 Python 环境报告未发现已知漏洞；不代表未知版本的生产环境也通过。

已完成一项同范围资源优化的对比：390×844、4 倍 CPU 降速、100ms 延迟、200,000 B/s 下载，关闭 HTTP 缓存及 SW，各测 3 次，LCP 为优化前 12.856/12.748/12.772s，优化后 6.196/5.564/5.492s；中位数 12.772s → 5.564s。首屏资源载荷 2,323,246 → 856,919 字节，CLS 均为 0。初次使用网络空闲作为完成条件有一轮超时，后改为首屏图片加载及渲染完成，未把超时记为通过。该实验仅证明本地同条件下的首屏改善；优化后的限速首屏仍需继续降低脚本/CSS传输和执行成本。

## 按优先级排列的已确认问题

### 1. 已修复 0.0.7：自动重试会清掉已经生成的场次

位置：[任务准备](backend/services/job_queue.py)、[生成工作流](backend/main.py)。

`OUTLINE_JOB` 在同一次执行中完成大纲和正文；第二次尝试进入 `prepare_job_attempt()` 后删除全部 Scene。隔离复现中，3 条已完成场次变成 0 条。额度在任务后期耗尽时，重试可先删掉已有成果，再在额度校验处失败。

调用放大：200 场电影的基线为 200 次大纲加 200 次正文；若每次接近末尾失败，默认 3 次任务尝试可接近 1,200 次语义调用。该计数不含 provider 重试或连续性重写，不是实际账单测量。

已实现：自动恢复按已持久化 `scene_index` 续跑，保留已完成场次、正文和摘要；明确 `auto` 全量重生成仍会建立快照后重置。隔离测试覆盖 3 条已完成场次的第二次任务尝试及中后期恢复。完整阶段/cursor 状态机仍在第 4 阶段。

### 2. 已修复 0.0.7：生成入口缺少正式设定完成和版本校验

位置：[generate_scenes](backend/main.py)。

`POST /projects/{id}/generate_scenes` 检查编辑权限和活动任务，但没有验证正式设定完整、确认状态或请求方 `context_revision`。隔离复现用 `pending` 类型和空 context，模拟掉 AI 梗概后，仍能入队并选择默认 5 场。

已实现：入队要求正式设定确认、完整字段和当前 `context_revision`，claim 后再次核验；前端自动生成携带当前版本。已有场次只有明确 `auto` 路径允许全量重生成。隔离测试覆盖 pending、空设定、旧版本和重复任务。

### 3. 已修复 0.0.7：任务租约没有约束场次写入

位置：[worker](backend/worker.py)、[任务租约](backend/services/job_queue.py)、[生成引擎](backend/main.py)。

心跳及任务终态更新检查 `lock_token`，生成引擎却没有接收该 token。旧 Worker 失去租约后，只要项目仍为 generating，就仍存在写 Project/Scene 的路径。取消任务也没有把取消信号传到当前 AI 请求及其后续重试。

已实现：Worker 将 job id/token 传入大纲/正文循环；provider 调用前后、循环边界和短事务业务写入均检查 `RUNNING + token + 未取消`。失租约/取消的已在途调用返回后只可写 stale 审计，不得写项目、场次或开始后续调用。多 Worker 真实 PostgreSQL 故障场景仍未实测。

### 4. 已修复 0.0.8：SQLite 与 PostgreSQL 的删除语义不一致

位置：[数据库连接](backend/database.py)、[模型](backend/models.py)、[删除项目](backend/main.py)。

应用隔离连接实测 `PRAGMA foreign_keys=0`。SQLite 允许审计日志保留悬空 `project_id`；开启外键后，带 AI 日志的项目删除会被外键拒绝。PostgreSQL 执行外键，而 `ai_logs.project_id` 尚未定义 `ON DELETE SET NULL`。

已实现：应用 SQLite 连接启用外键，迁移先将历史悬空 `ai_logs.project_id` 置空后建立 `ON DELETE SET NULL`，保留所有日志、用户与作品。旧 `upgrade_admin` 无项目外键表形也有升级测试。PostgreSQL 实库迁移和 downgrade 仍需单独补验。

### 5. P1/P2：LLM 重试、时限与预算边界不足

位置：[LLM 调用](backend/services/llm.py)、[协议适配](backend/services/llm_config.py)。

- 没有已知 usage 的非 HTTPException 异常可能执行 3 轮、每轮遍历全部启用档案；确定性的 provider 400/401/404 也会进入重试/fallback。
- 单档案超时允许 600 秒，但没有整个业务调用的总截止时间；未设置明确输出 token 上限。
- 联动整改的允许输入包含当前值、基线、before/after、说明、模板和 logline，极端合法组合约 292k 字符，尚未计 JSON 转义。
- 管理员一次连接测试可能先发 1 次再按并发数发最多 20 次真实请求，这些请求没有进入现有 AI 费用审计。

修复目标：统一调用预算与错误分类；确定性错误停止相同请求重试；输入、输出、总时限有配置及测试；管理员测试显示请求数量并记录真实已知用量。provider 能力与不同协议的参数需逐项核实。

### 6. 条件性 P1：公网开放注册时费用和队列准入不足

位置：[注册](backend/api/auth_routes.py)、[额度](backend/services/usage.py)、[用户模型](backend/models.py)。

公开注册创建的用户默认日/月额度均为 0（不限）；每用户项目数和排队任务数没有上限。单 Worker 串行处理整个项目，单一用户可通过多个项目占据 FIFO 队列。

已确认的是代码控制缺口。生产是否公网开放、是否存在外围访问控制尚未检查。注册方式、默认额度和每用户任务上限属于运营选择，需要用户明确决定，不能自行变更现有账户额度。

### 7. P2：密码哈希计算阻塞 API 事件循环

位置：[认证路由](backend/api/auth_routes.py)、[登录限流](backend/services/login_limiter.py)。

async 路由直接执行同步 bcrypt。隔离本机 5 次错误密码校验平均约 0.331 秒/次；同进程其他请求会受这段同步计算影响。限流键是 IP 与用户名组合，改变用户名可扩张键数；超过 10,000 个键时只清除已过期项。

修复目标：把哈希计算放入有容量约束的线程池，保留 dummy hash 和哈希强度；加入总量与 IP 层限流和容器容量上限。以并发认证期间轻量接口响应作为验收，不能仅比较密码计算速度。

### 8. 部分修复 0.0.8：用量聚合和权限查询放大

位置：[用量](backend/services/usage.py)、[项目列表](backend/main.py)、[任务列表](backend/api/operations_routes.py)。

- 当前 `COALESCE(billed_user_id,user_id)` 加时间条件的日/月统计没有匹配复合索引，隔离 SQLite 查询计划为 `SCAN ai_logs`。
- 一个成功首尝试的项目 AI 调用至少两次额度检查，每次日/月两次聚合，即至少 4 次全表扫描；fallback 再增加检查。
- 管理员 U 个用户的用量页为 `1 + 2U` 条 SQL。
- `/jobs` 先取全局最新 100 条再逐条权限过滤。隔离复现：用户有 1 条较旧可访问任务、其他人有 101 条新任务时，接口错误返回空；最多形成 `1 + 100` 次查询。
- 项目列表对共享项目逐条查询角色；队列热查询还产生临时排序。

已实现：用量日/月合并为一次聚合，`COALESCE(billed_user_id, user_id)` 表达式索引覆盖计费身份与时间；管理员用量使用一次分组结果。`/jobs` 在 SQL `LIMIT` 前按 owner 或有效 viewer/editor 成员过滤，消除 N+1；隔离查询计划和 101 条不可见任务反例通过。项目列表的共享成员 N+1 与分页尚未处理。

### 9. P2：生成轮询反复传输整份正文

位置：[前端轮询](frontend/src/App.vue)、[详情响应](backend/schemas.py)。

活动项目每 8 秒读取完整详情和任务列表；详情包含所有场次正文。后台存在生成项目时，每 45 秒读取未分页且带完整 global_context 的项目列表。成本随历史项目和正文大小增长。

修复目标：轮询返回状态、版本和变更场次标识，只有变化时读取正文；初次打开仍可按需加载完整作品。保留当前项目/版本请求守卫，禁止增量响应覆盖新项目或新草案。

### 10. P2：重复点击可多次发起付费输入审核

位置：[createProject / submitChoice](frontend/src/App.vue)。

开始创建和提交自由输入时，先等待 `/content/review`，之后才设置 loading。连点会产生多个审核请求；旧请求守卫阻止后续写入，却不能撤销已经发生的审核费用。

修复目标：审核前取得提交锁并禁用相应控件；finally 按请求身份释放。延迟模拟审核时，连点两次只能发出一次审核和一次后续写入。

0.0.6 进度：创建和提交选择已加入独立提交锁及按钮 loading/disabled；审核期间输入/选项受控，且回包校验输入快照以保留并发编辑。新增连点和后续编辑行为测试，独立审查通过。

### 11. P2：管理员日志参数与显示内容不一致

位置：[fetchSystemLogs / downloadSystemLogText](frontend/src/components/AdminDashboard.vue)。

在 Worker 日志请求未返回时切换 Backend，新请求因 loading 被跳过；旧 Worker 回包仍写入，下载动作也会取得旧内容。

修复目标：参数快照和请求序号绑定，过期回包忽略，最后一次参数变化确保被查询；仅当用户原本在底部时自动滚动，隐藏页面暂停自动刷新。

0.0.6 进度：已实现来源、行数、关键词快照与回包身份校验，限制下载为匹配参数的稳定内容，并处理隐藏/卸载。同参数在途请求复用，参数变化立即查询，避免慢网络下每个响应持续过期；行为测试与真实浏览器的来源/下载一致性验证通过。

### 12. P2：只读角色仍被引导到编辑接口

位置：[loadProject / regenerateScene / generateScenePrompt](frontend/src/App.vue)。

viewer 打开无场次的待设定项目会自动请求 analyze；完成场次仍显示重写和提示词操作。后端要求 editor，因此这些不是已确认的越权写入，而是权限呈现错误。两个场次操作也缺少完整的跨项目回包保护。

修复目标：viewer 不触发写接口，界面给出只读说明；编辑操作捕获项目、版本和请求身份。项目切换后旧结果不能提示新项目已成功或刷新错对象。

0.0.6 进度：已加入角色入口和场次请求隔离，删除入口限所有者；浏览器使用合成 viewer 验证不显示编辑按钮、无场次时展示只读说明且没有新增写请求。

### 13. P2/P3：大日志、全量版本和备份占用内存

位置：[管理员接口](backend/api/admin_routes.py)、[版本](backend/services/versions.py)、[备份](backend/services/backups.py)、[后台表单](frontend/src/components/AdminDashboard.vue)。

- 日志列表直接返回完整 prompt/response；单场重写前快照复制全部作品，正文大小 S、重写 R 次时版本约增加 R×S。
- 备份构造多份内存副本并同步压缩/加密，可能占用 API 事件循环。
- AI、备份、额度和 Prompt 等后台表单没有统一的离开未保存保护。
- API 与 Worker 启动时均执行 Alembic，服务启动顺序不能保证迁移互斥。

修复目标：日志概要与详情分开；备份采用临时文件和受控线程任务；迁移在服务启动前单一执行；后台表单基线与离开保护统一。历史版本/日志清理、保留期及数据删除需要单独批准。

## 依赖与静态资源

| 对象 | 初查证据 | 本项目适用判断 |
|---|---|---|
| browserslist | 2 个 high 公告 | 构建依赖；未证实公网输入可达该调用 |
| nanoid | high 公告，零长度 custom generator 可不终止 | 传递依赖；需核对具体调用，不直接等同线上高风险 |
| DOMPurify | moderate，IN_PLACE 与 hook 相关 XSS 公告 | 当前代码清洗字符串，未使用该选项；仍建议兼容补丁升级 |
| qs | 2 个 moderate 公告 | Express 传递依赖，需补丁升级及代理回归 |
| postcss-selector-parser | low，递归导致拒绝服务 | 构建工具输入面 |
| Node 默认安装版本 | 上轮草稿选用 22.12.0 | 本批改为已核实的 [22.23.2 安全版本](https://nodejs.org/en/blog/release/v22.23.2)，版本满足构建要求不等于任意旧补丁均安全 |
| Logo | 原图 1,494,583 字节，小图 22,916 字节 | 本批复用 192px 图标显示 96px/32px，移除大图必需缓存；原图保留 |

npm 公告来源：[Browserslist](https://github.com/advisories/GHSA-c83g-rgw3-j3cx)、[nanoid](https://github.com/advisories/GHSA-2v37-7h3g-55p8)、[DOMPurify](https://github.com/advisories/GHSA-55q2-fjhq-7xh7)、[qs](https://github.com/advisories/GHSA-4mjr-xmp4-gh2g)、[selector parser](https://github.com/advisories/GHSA-w9m9-85wc-3x92)。这是检查时依赖状态，不是后续升级后的结论。

0.0.6 已按现有 semver 范围更新上述包：DOMPurify 3.4.14、Browserslist 4.28.9、nanoid 3.3.18、qs 6.16.0、selector parser 6.1.4，并连带更新 5 个浏览器数据/维护包。安装禁用生命周期脚本；npm 随后报告 0 个已知漏洞。尚未据此推断所有代码路径安全。

实际运行 `frontend/server.cjs` 的另一个性能发现：对入口 JS/CSS 发出 `Accept-Encoding: gzip, br`，响应未设置 Content-Encoding，Cache-Control 为 `public, max-age=0`。0.0.5 的这组入口共 834,003 字节，等价 gzip 为 245,113 字节。后续可在静态资产服务提供压缩与哈希文件长期缓存；HTML/SW 需保持更新可见，API 不能沿用静态缓存策略。这是实测载荷与配置结果，尚未实施服务变更。

## 建议执行顺序与写入边界

| 阶段 | 最小范围 | 验收条件 | 状态 |
|---|---|---|---|
| 0. 移动端/PWA 收尾 | 现有前端、构建脚本、图标、文档 | 缓存白名单、双标签安全更新、未保存保护、手机实看、独立复审、0.0.5 同步 | 已完成，40eeb08 |
| 1. 生成与认证止损 | `main.py`、LLM 边界、队列准备；认证模块独立并行 | 已有场次不被重试清空；未确认/旧设定不能入队；确定性错误不重复；认证不阻塞事件循环 | 生成/队列止损 0.0.7 已同步；LLM 与认证未开始 |
| 2. 数据与读取性能 | 模型、迁移、usage、列表接口；前端增量轮询随后集成 | 外键规则明确且兼容；权限先过滤；索引生效；轮询不传未变化正文 | 外键、用量、jobs 0.0.8 已同步；项目列表与增量轮询未开始 |
| 3. 工作流与 UI | App 与 Admin 分别独占；安全依赖升级 | 连点无重复审核；viewer 只读；日志最新参数生效；补丁回归；后台表单未保存保护另行实施 | 局部修复 0.0.6 已同步 ac296f1；后台表单未开始 |
| 4. 生成状态机 | 单一负责人提取生成工作流，worker/queue 串行集成 | 阶段及恢复位置持久化；业务写入受租约约束；旧 worker 不能写；取消后不新增调用 | 租约围栏 0.0.7 已同步；阶段/cursor 状态机未开始 |
| 5. 运维大对象 | 备份/版本/管理员 API | 日志详情按需；备份内存和事件循环测量；迁移单一执行 | 未开始 |

分工建议：复杂生成边界与状态机由 Sol/xhigh 或 ultra 负责；认证、查询、UI 和测试用 Terra/high；独立审查由未参与实现的 Sol/xhigh 负责。每批只安排独立文件并行，`main.py`、App、schema 与迁移串行集成。每个完成批次更新可见版本、文档及既有 `main` 分支，推送后核对远端。

需要用户决定：公网注册策略、普通账户默认额度、每用户队列/项目上限；已有悬空审计引用的迁移处理；新配置的业务调用总时限和输出预算；任何历史版本/日志保留或清理策略。不自行更改既有用户额度或删除历史作品。

架构保留：Vue/FastAPI、数据库持久队列、双 CAS、工作稿/正式设定隔离、最多一次选项补齐、owner/actor 费用分离和现有权限层级。当前没有证据要求引入 Redis/Celery 或整体重写；提取生成状态机是为了明确阶段、恢复、取消与事务边界。

## 本轮交付证据

- 0.0.5：移动头部、PWA、安全更新、构建停止条件、首屏图片，提交 `40eeb08` 已核对远端。
- 0.0.6：提交防重复与输入保护、只读与所有者入口、跨项目场次响应、日志竞态和慢网去重、安全补丁；独立审查通过，前端 67/67、生产构建、SW 14 项校验通过。18 个相关生命周期用例另由独立审查者复验。提交 `ac296f1` 已推送；本地 HEAD、origin/main 与 `git ls-remote` 均为 `ac296f1474e0db9818f62abdd657f154f7472350`。
- 0.0.7：生成准入、自动重试保留已完成场次、Worker 租约/取消围栏和失租约审计边界，提交 `e078946` 已推送并核对远端。后端 385 项、前端 67 项、生产构建与 SW 校验通过，独立审查通过。
- 0.0.8：SQLite 外键、历史审计引用修复、用量表达式索引、管理员聚合和 `/jobs` 权限分页，提交 `26f76d3` 已推送并核对远端。后端 385 项、前端 67 项、生产构建与 SW 校验通过，独立审查通过。
- 真实 Chromium 验证：320–1280px 横排品牌与触控区域；登录、移动抽屉、桌面及后台同为 0.0.6；Manifest 无错误；实际 Cache Storage 无 API；双标签更新保留另一标签输入；明确放弃只刷新一次、无二次原生提示；viewer 无写操作；日志切换后显示与下载均为最新来源。
- 实验采用本地合成数据和独立浏览器，临时服务已停止。没有生产部署、真实收费 AI、实体 iPhone 安装或 PostgreSQL 实库验收。
- 本报告随仓库保留，供后续决策和分批开发使用；已修复项与未实施项分别标记，不能把后续方案当作现有能力。
